# Docker: `docker compose` loads this file via docker-compose.yml `env_file`.

# OpenAI — used by ChatOpenAI / LangChain when llm_provider="openai"
OPENAI_API_KEY=

# Google Gemini — used when llm_provider="gemini" (AI Studio key)
GOOGLE_API_KEY=
